// Problema 2.1

// let b = 10, h = 5, area = 0;
// area = b * h / 2;
// console.log(`El área del triángulo es de ${area} m2`);


// Problema 2.2

// let soles = 1000, dolares = 0, cambio = 3.7;
// dolares = soles / cambio;
// console.log(`Dólares a comprar: ${dolares}`);


// Problema 2.3

// let nacimiento = 1982, actual = 2021, edad = 0;
// edad = actual - nacimiento;
// console.log(`Edad: ${edad} años`);


// Problema 2.4

// let horas = 3.5, precio = 5, total = 0;
// total = Math.ceil(horas) * precio;
// console.log(`Pago total es de ${total} soles`);


// Problema 2.5

// let m2 = 250, precio = 5, total = 0;
// total = m2 * precio;
// console.log(`Pago total es de ${total} soles`);


// Problema 2.6

// let a = 3, b = 4, h = 0;
// h = Math.sqrt(a*a + b*b);
// console.log(`La hipotenusa es: ${h}`);


// Problema 2.7

// let km = 10, precio = 5, total = 0;
// total = km * precio;
// console.log(`La precio es: ${total} soles`);


// Problema 2.8

// let velocidad = 10, distancia = 5, tiempo = 0;
// tiempo = distancia / velocidad;
// console.log(`El tiempo es de ${tiempo} horas`);


// Problema 2.9

// let tiempo = 10, precio = 1, total = 0;
// total = tiempo * precio;
// console.log(`El total es de ${total} soles`);


// Problema 2.10

// let agua = 10, precio = 0.5, total = 0;
// total = agua * precio;
// console.log(`El total es de ${total} soles`);

// Problema 2.11

// let luz = 80, precio = 0.5, total = 0;
// total = luz * precio;
// console.log(`El total es de ${total} soles`);


// Problema 2.12

// let precio = 100, iva = 0.15, total = 0;
// total = precio * 0.8;
// console.log(`El precio con descuento es de ${total} soles`);
// total = total + total * iva;
// console.log(`El precio final con iva es de ${total} soles`);


// Problema 2.13

// let ahorro = 0, sueldo = 1000;
// ahorro = 12 * 4 * sueldo * 0.15;
// console.log(`El ahorro en 1 año es de ${ahorro} soles`);


// Problema 2.14

// let hotel = 500, comida = 100, otros = 100, dias = 2, cheque = 0;
// cheque = (hotel + comida + otros) * dias;
// console.log(`El cheque por ${dias} dias es de ${cheque} soles`);


// Problema 2.15

// let v = 12, r = 4, p = 0;
// p = v * v / r;
// console.log(`La potencia es de ${p} watts`);


// Problema 2.19

// let b = 10, h = 5, area = 0;
// area = b * h;
// console.log(`El área del cuadrado es de ${area} m2`);


// Problema 2.20

// let n1 = 4, n2 = 4, n3 = 20, promedio = 0;
// promedio = (n1*0.25 + n2*0.25 + n3*0.5);
// console.log(`El promedio es: ${promedio}`);


// Problema 2.21

// let edad = 38, meses = 0, semanas = 0, dias = 0, horas = 0;
// meses = edad*12;
// semanas =  meses * 4;
// dias = semanas * 7;
// horas = dias * 24;
// console.log(`Ha vivido ${meses} meses o ${semanas} semanas o ${dias} dias u ${horas} horas`);


// Problema 2.22

// let tiempo = 10, precio = 1, total = 0;
// total = tiempo * precio;
// console.log(`El total es de ${total} soles`);


// Problema 2.23

let precio = 100, dias = 5, total = 0;
total = dias * precio;
console.log(`El total es de ${total} soles`);